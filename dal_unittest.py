# #!/usr/bin/env python3


# from model.bank import Bank
# from dal.bankDAL import BankDAL

# if __name__ == '__main__':
	# bank = Bank(1, 'bankname2')

	# print(bank.name)
	# dal = BankDAL()
	# #dal.print_model()
	# dal.insert(bank)
	# bank = dal.select(2)
	# if bank != None:
		# bank.name = 'bank24'
		# dal.update(bank)
	# banks = dal.select_all()
	# for b in banks:
		# print('{0}-{1}'.format(b.id, b.name))

	# if bank != None:
		# dal.delete(bank)
		# banks = dal.select_all()
		# for b in banks:
			# print('{0}-{1}'.format(b.id, b.name))
